# Deploying Atlas v6.0.0 on Amazon Web Services (AWS EKS / ECS)

## Deployment Options
- **Amazon EKS**: Deploy using Helm chart (`atlas-infrastructure/helm/atlas`).
- **Amazon ECS / Fargate**: Deploy using Docker Compose (`docker-compose.prod.yml`) via `ecs-cli`.
