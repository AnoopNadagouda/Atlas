package com.atlas.domain.graph;

public enum RelationType {
    USES,
    CREATED_BY,
    WORKS_FOR,
    PART_OF,
    LOCATED_IN,
    DEPENDS_ON,
    IMPLEMENTS,
    CALLS,
    IMPORTS,
    MENTIONS,
    REFERENCES
}
